// import React from "react";
import styled from "styled-components";

const Login = (props) => {
  return (
    <Container>
      <Content>Contenet</Content>
    </Container>
  );
};

const Container = styled.section`
  overflow : hidden;
  display:flex;
  flex-diraction:column;
  text-align:center;
  hight:100vh;

`
const Content =styled.div`
  margin-bottom : 10vw;
  

`
export default Login;
